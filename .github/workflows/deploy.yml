name: Deploy static content to Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          
      - name: Install dependencies
        run: |
          npm uninstall vite
          npm install vite@5.4.11 --save-dev
          npm install

      - name: Environment Debug
        run: |
          node -v
          npm -v
          ls -la node_modules/.bin/vite

      - name: Build
        run: npx vite build --outDir dist --debug
        
      - name: Verify Build Output
        run: ls -la dist

      - name: Confirm New Workflow
        run: echo "Using Manual Tar Strategy with upload-artifact@v4"

      - name: Tar dist directory
        run: tar -cvf artifact.tar -C dist .

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: github-pages
          path: artifact.tar
          retention-days: 1
          
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
